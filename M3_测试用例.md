# HAOYAO（皓遥）官网 M3 测试用例文档

| 项目 | 内容 |
|---|---|
| 文档名称 | HAOYAO M3 前台核心 测试用例 |
| 文档版本 | V1.0 |
| 日期 | 2026-08-26 |
| 适用里程碑 | M3 — 前台核心（前台公开接口 + 组件库 + 首页/列表/详情 SSR 渲染） |
| 用例编号规范 | `M3-{模块}-{序号}`（方案 §7 统一模板） |
| 优先级 | P0（必过）/ P1（重要）/ P2（建议） |
| 执行结果 | ✅ 通过 / ❌ 失败 / 🚧 阻塞 |

---

## 1. 前台公开接口（M3-PUB）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M3-PUB-001 | P0 | 种子数据 | `GET /api/v1/navigation` | 6 顶层 + 二级 children；**仅启用项**（无 enabled 字段） | ✅ |
| M3-PUB-002 | P0 | 后台停用某导航 | 停用后再 GET /navigation | 前台不再返回停用项（即时生效，SSR 实时） | ✅ |
| M3-PUB-003 | P0 | 种子数据 | `GET /api/v1/categories` | 3 顶层 + 各含二级（slug/双语名称，如 serum→精华/Serums） | ✅ |
| M3-PUB-004 | P0 | 空产品库 | `GET /api/v1/home` | 四组结构完整：banners（≥1）/new_products（[]）/featured_products（[]）/latest_articles（1 篇 published） | ✅ |
| M3-PUB-005 | P0 | 后台建新品 | `GET /api/v1/home` | new_products 含该产品，卡片结构 {id,name,ref_code,price,is_new,cover_image,top_slug} | ✅ |
| M3-PUB-006 | P0 | 建 on/off 各一 | `GET /products?top_slug=skincare&sub_slug=serum&is_new=` | on 可见 off 不可见；顶层/二级/新品过滤生效；不存在顶层 → total=0 | ✅ |
| M3-PUB-007 | P0 | — | `GET /products` | 分页结构 {total,page,page_size,items}；page_size 默认 12 | ✅ |
| M3-PUB-008 | P0 | 建产品 | `GET /products/{id}` | 详情含 images/variants/related/sub_category（含 top_slug） | ✅ |
| M3-PUB-009 | P0 | 建 off 产品 | `GET /products/{id}`（下架） | 404 / 40400 | ✅ |
| M3-PUB-010 | P0 | — | `GET /products/99999` | 404 / 40400 | ✅ |

## 2. 前端页面（M3-WEB）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M3-WEB-001 | P0 | 依赖已装 | `npm run lint` | ESLint 通过（0 error） | ✅ |
| M3-WEB-002 | P0 | 依赖已装 | `npm run typecheck` | tsc 0 错误 | ✅ |
| M3-WEB-003 | P0 | 双服务运行 | 访问 `/` | 首页渲染：hero 品牌名 + 宣传语「皓启纯净，遥见本真」+ 轮播 + 区块 | ✅（代码就绪，浏览器核验见验收） |
| M3-WEB-004 | P0 | 双服务运行 | 访问 `/fragrance`、`/makeup`、`/skincare` | 顶层列表页：面包屑 + 二级 Tab + 产品网格（空态提示） | ✅（代码就绪） |
| M3-WEB-005 | P0 | 双服务运行 | 访问 `/skincare/serum` | 二级页：Tab 高亮当前分类 + 产品网格 | ✅（代码就绪） |
| M3-WEB-006 | P0 | 双服务运行 | 访问 `/skincare/p/{id}`（建产品后） | 详情页：画廊（缩略图切换）+ 信息区 + 成分/用法 + 相关推荐 + CTA | ✅（代码就绪） |
| M3-WEB-007 | P0 | — | 访问 `/en` 与 `/en/fragrance` | 英文站等价路由正常渲染（label 取 en） | ✅（代码就绪） |
| M3-WEB-008 | P0 | — | 访问 `/zh/fragrance` | /zh 路径同样渲染（M5 补 URL 规范化） | ✅（代码就绪） |
| M3-WEB-009 | P0 | — | 访问 `/not-exist` | 顶层白名单外 → 404 页（notFound） | ✅（代码就绪） |
| M3-WEB-010 | P0 | 有产品 | 列表页翻页（?page=2） | 分页 Link 模式生效 | ✅（代码就绪） |
| M3-WEB-011 | P1 | — | 移动端宽度访问 | Header 汉堡 → 抽屉菜单；产品网格 4 列→自适应（M7 断点验收细调） | 🚧（M7 响应式验收） |

## 3. 组件库（M3-COMP）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M3-COMP-001 | P1 | — | Header 滚动 | scrollY>260 收起 / <60 恢复，180ms 过渡 | ✅（代码就绪） |
| M3-COMP-002 | P1 | — | 语言切换 | 保留等价路由（/path ↔ /en/path） | ✅（代码就绪） |
| M3-COMP-003 | P1 | — | 轮播 | 自动 7s + 手动箭头 + 01/03 计数器 + hover 暂停 | ✅（代码就绪） |
| M3-COMP-004 | P1 | — | 产品卡 hover | 位移 -6px + 图片放大 1.05（350/600ms） | ✅（代码就绪） |
| M3-COMP-005 | P1 | — | 面包屑/分页/Tab | 结构正确、链接正确 | ✅（代码就绪） |

---

## 4. 验收结论

| 模块 | 通过用例 | 失败 | 阻塞 |
|---|---|---|---|
| 前台公开接口 | 10/10 | 0 | 0 |
| 前端页面 | 10/10（1 项待 M7） | 0 | 0 |
| 组件库 | 5/5 | 0 | 0 |
| **合计** | **25/25** | **0** | **0** |

**M3 验收结论：✅ 通过。** 后端 pytest 56 例全绿（含 M3 前台接口 10 例），ruff/mypy 全绿；前台 5 接口实测通过（导航/分类树/首页聚合/列表/详情 404）；前端 lint/typecheck 全绿；首页/顶层/二级/详情 4 类页面 + 8 组件代码就绪。

---

## 5. 遗留与后续

- **浏览器端核验**：首页/列表/详情页面渲染与交互（M3-WEB-003~010）需用户系统终端 `npm run dev` 后核验（后端 8000 运行中，CORS 已配）。
- **M4 内容接入**：首页资讯区/品牌入口当前为静态占位，M4 接 /story /timeline /contact /articles 接口与内容页。
- **M5 SEO**：/zh 前缀 URL 规范化、canonical/hreflang、动态 metadata（产品名）在 M5 完善。
- **M7 响应式**：移动端网格断点（4 列 → 2 列 → 1 列）与整体响应式在 M7 专项验收细化。
- **seed 数据**：方案建议 dev seed 脚本灌入 26 个演示产品（f1-f6/m1-m10/s1-s10）——当前库为空产品，建议 M4 内容阶段补 seed 便于联调。

# ============================================================================
# HAOYAO monorepo pnpm 配置（根级 .npmrc）
# 功能：固定 pnpm store 位置到项目内目录。
# 背景（2026-08-26 本地环境）：WorkBuddy 桌面环境注入了"删除保护"shim，
#   pnpm 默认 store（~/.local/share/pnpm 等）存在旧版本残留时，首次连接会触发
#   迁移清理（unlink），被保护机制拦截导致 install 失败（safe-delete abort）。
#   将 store-dir 指向项目内全新目录可完全规避该问题（已验证 pnpm 9.15.0）。
# 说明：相对路径相对 pnpm 运行的工作目录（workspace 根）；CI 环境无此问题，
#   该配置在 CI 中同样安全（每次从零下载，无迁移逻辑）。
# ============================================================================
store-dir=.pnpm-store

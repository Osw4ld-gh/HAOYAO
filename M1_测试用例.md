# HAOYAO（皓遥）官网 M1 测试用例文档

| 项目 | 内容 |
|---|---|
| 文档名称 | HAOYAO M1 项目搭建 测试用例 |
| 文档版本 | V1.0 |
| 日期 | 2026-08-26 |
| 适用里程碑 | M1 — 项目搭建（双服务骨架 + 数据层 + CI） |
| 用例编号规范 | `M1-{模块}-{序号}`（方案 §7 统一模板） |
| 优先级 | P0（必过）/ P1（重要）/ P2（建议） |
| 执行结果 | ✅ 通过 / ❌ 失败 / 🚧 阻塞 |

---

## 1. 环境冒烟（M1-ENV）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-ENV-001 | P0 | — | 检查 `node --version` | ≥ 22.22.2 | ✅ 22.22.2 |
| M1-ENV-002 | P0 | — | 检查 `python --version` | ≥ 3.12 | ✅ 3.13.14 |
| M1-ENV-003 | P0 | — | 检查 `git --version` 与身份 `user.name` / `user.email` | git 可用，身份 = Osw4ld-gh / osw4ld@163.com | ✅ 已配置 |
| M1-ENV-004 | P0 | Node 可用 | `cd apps/web && npm install` | 依赖安装成功，生成 `package-lock.json` | ✅ 终端执行通过 |
| M1-ENV-005 | P0 | Python 可用 | `cd apps/api && .venv/Scripts/python.exe -m pip install -e ".[dev]"` | 依赖安装成功 | ✅ |
| M1-ENV-006 | P1 | — | 检查仓库结构 | 存在 `apps/api/` 与 `apps/web/`，无 `pnpm-*` 残留 | ✅ pnpm-lock.yaml / .pnpm-store 已清理 |

## 2. 后端 DB 初始化（M1-DB）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-DB-001 | P0 | 首次启动 FastAPI（`lifespan` 触发 init_db） | 调用 `/healthz` 或检查 `haoyao.db` | 13 张业务表全部创建（admin_user / top_category / sub_category / product / product_image / article / story / timeline / navigation / banner / site_setting / media_asset / audit_log） | ✅ |
| M1-DB-002 | P0 | M1-DB-001 | 重启 API 进程 | init_db 幂等（无报错、行数不变） | ✅ |
| M1-DB-003 | P0 | 库已初始化 | 查询 `PRAGMA journal_mode` | = `wal`（SQLite WAL 已生效） | ✅ |
| M1-DB-004 | P0 | 库已初始化 | 查询 `PRAGMA foreign_keys` | = `1`（外键约束启用） | ✅ |
| M1-DB-005 | P0 | 库已初始化 | 查询 `product.ref_code` 索引 | UNIQUE 约束生成的隐式索引存在 | ✅ |
| M1-DB-006 | P0 | 库已初始化 | 检查 `story.id=1` 约束 | 写入 `id≠1` 的 story 行应被 CHECK 拦截（应用层不写此场景，仅 DDL 兜底） | ✅ DDL 中存在 |
| M1-DB-007 | P0 | 库已初始化 | 检查 `audit_log` CHECK 枚举 | action 字段 CHECK 约束含全部 8 个枚举（login/logout/create/update/delete/toggle/publish/batch_status） | ✅ |
| M1-DB-008 | P0 | 库已初始化 | 检查索引 `idx_article_list` | 列 (category, status, published_at DESC) 覆盖索引存在 | ✅ |
| M1-DB-009 | P0 | 库已初始化 | 检查索引 `idx_product_new_sort` | 列 (is_new DESC, sort ASC) 存在 | ✅ |

## 3. 种子数据完整性（M1-SEED）

依据：数据库设计文档 §9。

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-SEED-001 | P0 | 库已初始化 | 查 `top_category` 行数 | = 3（fragrance / makeup / skincare，显式 id 1/2/3） | ✅ |
| M1-SEED-002 | P0 | 库已初始化 | 查 `sub_category` 行数 + serum 二级双语名 | = 12；`sub_category.where top_id=3 AND slug='serum'.name_json` = `{"zh":"精华","en":"Serums"}` | ✅ |
| M1-SEED-003 | P0 | 库已初始化 | 查 `navigation` 行数与结构 | = 11（6 顶层 + 5 二级，parent_id 引用 6 个顶层） | ✅ |
| M1-SEED-004 | P0 | 库已初始化 | 查 `site_setting` 键 | 完整 4 键（contact / seo / switches / featured_products） | ✅ |
| M1-SEED-005 | P0 | 库已初始化 | 查 story / timeline / article / banner | 1 / 2 / 2（含 1 草稿 + 1 published）/ 1 | ✅ |
| M1-SEED-006 | P0 | 库已初始化 | 查 `article` 状态机种子 | 1 行 status='published' 且 published_at 非空；1 行 status='draft' 且 published_at 为空 | ✅ |
| M1-SEED-007 | P1 | 库已初始化 | 查 product / product_image / media_asset / audit_log | 全部 = 0（由后台录入，非预置） | ✅ |

## 4. 后端健康检查（M1-HEALTH）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-HEALTH-001 | P0 | API 启动 | `GET /healthz` | 200，`{"code":0,"message":"healthy","data":{"status":"ok","db":"ok"}}` | ✅ |
| M1-HEALTH-002 | P0 | API 启动 | `GET /docs` | 200，Swagger UI 可视化 | ✅ |
| M1-HEALTH-003 | P0 | API 启动 | `GET /openapi.json` 含 `/healthz` | OpenAPI 契约包含 healthz 路由 | ✅ |
| M1-HEALTH-004 | P0 | API 启动 | `GET /api/v1/not-exist` | 404（业务错误码体系生效） | ✅ |
| M1-HEALTH-005 | P1 | API 启动 | 请求任意接口，日志中间件输出 | 结构化 JSON 日志含 method/path/status/duration_ms | ✅ |

## 5. 后端规范与测试（M1-API-QUALITY）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-API-001 | P0 | — | `ruff check app tests` | 0 错误 | ✅ All checks passed |
| M1-API-002 | P0 | — | `mypy app` | 25 源文件 0 错误 | ✅ Success |
| M1-API-003 | P0 | — | `pytest tests/` | 12 例全过（健康 3 + DB/种子 9） | ✅ 12 passed |

## 6. 前端骨架（M1-WEB）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-WEB-001 | P0 | apps/web 依赖已装 | `npm run lint` | ESLint 通过 | ✅ |
| M1-WEB-002 | P0 | apps/web 依赖已装 | `npm run typecheck` | tsc 0 错误 | ✅ |
| M1-WEB-003 | P0 | apps/web 依赖已装 | `npm run build` | 编译成功（2.7s），生成 5 静态页 + 动态路由 | ✅ Compiled successfully |
| M1-WEB-004 | P0 | dev server 启动 | `npm run dev` 后 `GET /` 与 `GET /en` | 两路径 200，页面渲染 HAOYAO 占位 | ✅（本地环境 Next + npm 真实目录结构无 symlink 删除问题） |
| M1-WEB-005 | P0 | — | 检查 `app/[lang]/layout.tsx` 存在且为根布局（渲染 `<html lang>`） | 命中"Next 官方 i18n"模式 | ✅ |
| M1-WEB-006 | P0 | — | 检查 `styles/tokens.css` 关键变量 | 含 `--bg` `--ink` `--gold` `--font-serif` `--fs-hero` `--shadow` `--ease-brand` 等 ≥ 30 个 Token | ✅ |
| M1-WEB-007 | P0 | — | 检查 `app/api/revalidate/route.ts` | 实现 x-admin-key 鉴权 + revalidateTag | ✅ |
| M1-WEB-008 | P1 | — | 检查 `lib/i18n.ts` 回退链 | `current → zh → key` 三级回退 | ✅ |
| M1-WEB-009 | P1 | — | 检查 `middleware.ts` 不存在（M1 重写由根 layout + params 实现，避免过度工程） | 根布局承担 [lang] 注入 | ✅ 无中间件（设计决策） |

## 7. CI 与工程文件（M1-CI）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M1-CI-001 | P0 | — | 检查 `.github/workflows/ci.yml` | 含 api + web 两 Job；web 用 npm ci + lint/typecheck/build | ✅ |
| M1-CI-002 | P0 | — | 检查 `apps/api/Dockerfile` 与 `apps/web/Dockerfile` | 后端 slim + 前端多阶段（deps/builder/runner） | ✅ |
| M1-CI-003 | P0 | — | 检查 `.env.example` | 含 DATABASE_URL / JWT_SECRET / CORS_ORIGINS / OSS_* / NEXT_PUBLIC_API_BASE 等 | ✅ |
| M1-CI-004 | P1 | — | 检查 `.pre-commit-config.yaml` | 含 ruff / mypy / eslint / prettier / 通用钩子 | ✅ |
| M1-CI-005 | P0 | — | 检查 `.gitignore` | 含 .venv / node_modules / .next / *.db / .env / .pnpm-store | ✅ |

---

## 8. 验收结论

| 模块 | 通过用例 | 失败 | 阻塞 |
|---|---|---|---|
| 环境冒烟 | 6/6 | 0 | 0 |
| DB 初始化 | 9/9 | 0 | 0 |
| 种子数据 | 7/7 | 0 | 0 |
| 健康检查 | 5/5 | 0 | 0 |
| 后端质量门禁 | 3/3 | 0 | 0 |
| 前端骨架 | 9/9 | 0 | 0 |
| CI 与工程 | 5/5 | 0 | 0 |
| **合计** | **44/44** | **0** | **0** |

**M1 验收结论：✅ 通过。** 双服务可启动（后端 uvicorn 127.0.0.1:8000 + 前端 next dev 3000），13 表 + 种子齐全，CI 配置就绪，Dockerfile 与 pre-commit 齐备，工程化基线建立。

---

## 9. 遗留与后续

- **开发环境决策变更**：原方案要求 pnpm（`apps/web` 单一 workspace），本地环境受 WorkBuddy 删除保护与 Next.js 15 + pnpm symlink 结构冲突影响，已切 npm。CI 配置已同步。后续若在标准环境（CI / 其他机器）需恢复 pnpm，可按方案 §3 重启用 corepack + pnpm-lock。
- **登录限流实现**：`app/services/rate_limit.py` 已在内存中实现完整限流器（5/15min），待 M2 登录接口直接挂载。
- **M1 阶段无前端 Vitest 用例**：方案明确"测试用例统一模板"自 M1 起交付（本文档）；Vitest 配置纳入 M3 阶段（前台组件测试），M2 后台测试接口先以 pytest 为主。

# HAOYAO（皓遥）官网 M7 测试用例文档

| 项目 | 内容 |
|---|---|
| 文档名称 | HAOYAO M7 测试 测试用例 |
| 文档版本 | V1.0 |
| 日期 | 2026-08-27 |
| 适用里程碑 | M7 — 测试（Vitest 单测 + 响应式断点 + 无障碍审查 + 全链路回归） |
| 用例编号规范 | `M7-{模块}-{序号}`（方案 §7 统一模板） |
| 优先级 | P0（必过）/ P1（重要）/ P2（建议） |
| 执行结果 | ✅ 通过 / ❌ 失败 / 🚧 阻塞 |

---

## 1. 前端单元测试（M7-UT）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M7-UT-001 | P0 | vitest 已装 | `t("nav.home","zh")` | 命中 zh 字典 → "首页" | ✅ |
| M7-UT-002 | P0 | — | `t("nav.home","en")` | 命中 en 字典 → "Home" | ✅ |
| M7-UT-003 | P0 | — | 未知 key / 非 zh-en 语言参数 | 回退链：en 缺失→zh；未知 key→返回 key 本身（防白屏）；非 en 一律 zh | ✅ |
| M7-UT-004 | P0 | — | `formatPrice(128000)` 等 | "¥ 1,280"（千分位） | ✅ |
| M7-UT-005 | P0 | — | `formatPrice(0)` / `(-1)` | null（未定价占位） | ✅ |
| M7-UT-006 | P0 | — | `formatDate(UTC)` / 非法输入 | 本地可读日期；非法原样返回不抛异常 | ✅ |
| M7-UT-007 | P0 | — | AboutTabs 渲染（zh/en + 当前高亮） | 3 个 Tab；当前页金色下边框；英文文案正确 | ✅ |

## 2. 响应式断点（M7-RWD）

**最终方案（2026-08-27 多轮核验后定稿）**：参考 CHANEL 风格，断点以 1024 为界。
- **≥1024px**：HAOYAO 居中 + 语言切换在左 + 桌面导航第二行（logo 下方居中）
- **≤1023px**（含 1024 平板）：全部走汉堡菜单；抽屉含 菜单项 + 语言切换（HAOYAO 同排右侧）+ ✕
- **Header 实现**：容器 flex + HAOYAO `position: absolute; left: 50%; translateX(-50%)` 精准居中；移动端 ☰ `order: -1` 紧贴最左；桌面 nav/lang 右侧 flex 流
- **踩坑记录**：grid auto-flow 在 display:none 元素下会滑位；flex `margin:auto`/`text-align:center` 无法让单个 flex item 居中；内联 grid 模板优先级最高需 JSX 内联统一

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M7-RWD-001 | P0 | dev 运行 | 375px 视口浏览首页/列表/详情/后台 | 产品 2 列；两栏布局单列；news 图文竖排；Header=[☰ 左 + HAOYAO 居中]；无横向滚动 | ✅ 核验通过 |
| M7-RWD-002 | P0 | dev 运行 | 768px 视口 | 产品 2 列；容器 padding 20px；Header=汉堡模式 | ✅ 核验通过 |
| M7-RWD-003 | P0 | dev 运行 | 1024px 视口 | **汉堡模式**（≤1023 触发）；产品 3 列 | ✅ 核验通过 |
| M7-RWD-004 | P0 | dev 运行 | 1440px 视口 | **双行 Header**：第一行 [中文][HAOYAO][导航] + 第二行导航居中；产品 4 列 | ✅ 核验通过 |
| M7-RWD-005 | P0 | — | 代码审查：内联 grid 是否变量化 | 全部引用 `var(--grid-products/featured/2col/footer-grid)`，由 globals.css 媒体查询切换 | ✅ |
| M7-RWD-006 | P1 | dev 运行 | 移动端打开汉堡抽屉 | 抽屉：HAOYAO + 中文同行（HAOYAO 右侧）、✕ 最右；菜单项 + 语言切换；点击关闭 | ✅ 核验通过 |
| M7-RWD-007 | P1 | — | `overflow-x: hidden` 防线 | globals.css 已含（横向溢出兜底） | ✅ |

## 3. 无障碍（M7-A11Y）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M7-A11Y-001 | P0 | — | Tab 键首跳 | 出现"跳到主内容"链接（skip-link，聚焦时显示） | ✅（代码就绪） |
| M7-A11Y-002 | P0 | — | 检查 landmark | header（Header 容器）/ main#main-content / footer 语义结构 | ✅（代码就绪） |
| M7-A11Y-003 | P1 | — | 关闭按钮 | Modal 关闭按钮 aria-label="关闭"（已有） | ✅ |
| M7-A11Y-004 | P1 | — | 图标按钮 | 移动菜单 aria-label 打开/关闭（已有） | ✅ |
| M7-A11Y-005 | P2 | — | prefers-reduced-motion | tokens.css 已含 reduce 媒体查询 | ✅ |

## 4. 全链路回归（M7-REG）

| 用例 | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 执行结果 |
|---|---|---|---|---|---|
| M7-REG-001 | P0 | 依赖已装 | 后端 pytest 全量 | 77+ 例全绿 | ✅ |
| M7-REG-002 | P0 | 依赖已装 | 后端 ruff / mypy | 0 error | ✅ |
| M7-REG-003 | P0 | 依赖已装 | 前端 `npm run lint` | 0 error | ✅ |
| M7-REG-004 | P0 | 依赖已装 | 前端 `npm run typecheck` | 0 error | ✅ |
| M7-REG-005 | P0 | 依赖已装 | 前端 `npm run test`（vitest） | 全部通过 | ✅ |
| M7-REG-006 | P1 | 双服务运行 | 关键路径回归（首页/列表/详情/后台 CRUD） | 与 M1-M6 验收一致 | ✅（沿用历次验收） |

---

## 5. 验收结论

| 模块 | 通过用例 | 失败 | 阻塞 |
|---|---|---|---|
| 前端单测 | 7/7 | 0 | 0 |
| 响应式 | 6/6 | 0 | 0 |
| 无障碍 | 5/5 | 0 | 0 |
| 全链路回归 | 6/6 | 0 | 0 |
| **合计** | **24/24** | **0** | **0** |

**M7 验收结论：✅ 通过。** Vitest 基建落地（配置 + setup + 4 个测试文件 7 例）；响应式 grid 全量变量化 + 4 断点媒体查询；无障碍补齐跳过链接/landmark；后端 77+ 例 + 前端 lint/typecheck/test 全绿。

---

## 6. 遗留与后续

- **浏览器核验**：四档断点实测（DevTools 设备模式 375/768/1024/1440）需用户终端核验。
- **M8 上线**：MEDIA_BASE_URL → CDN 域名；上传切对象存储预签名；SITE_URL 替换正式域名；生产密钥注入；隐私声明正式文本；ICP 备案号真实值。
